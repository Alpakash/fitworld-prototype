import React from 'react'
import gql from "graphql-tag"
import { useMutation } from '@apollo/react-hooks'
import { Mutations } from '../lib/index';

// Mutation

const Mutation = () => {
  let input;

  return (
    <>
    </>
  );
}

export default Mutation